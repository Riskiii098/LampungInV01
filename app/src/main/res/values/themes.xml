<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LampungInV01" parent="Theme.Material3.Light.NoActionBar">
        <!-- Mengubah warna background aplikasi -->
        <item name="android:windowBackground">@color/soft_yellow_white</item>
        <item name="colorSurface">@color/soft_yellow_white</item>
        <item name="android:windowLightStatusBar">true</item>g

    </style>
    <style name="Theme.LampungInV01" parent="Base.Theme.LampungInV01" />

    <style name="RoundedTopImage">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">12dp</item>
        <item name="cornerSizeTopRight">12dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>
</resources>